# Local Bootstrap Scratch Space

Use this directory only for local, synthetic-only layout experiments. It is not a source of truth for production templates and should never contain a real-person record, a source PDF path, or extracted clinical text.

If a bootstrap result becomes useful, review it for synthetic-only content and move the finalized template into `../pdfme`, `../html`, or `../overlay` before adding it to the catalog manifest.
